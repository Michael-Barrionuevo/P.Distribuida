name: telemetria
services:
  db-server:
    image: postgres:17.10-alpine3.24
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Badboy22
      POSTGRES_DB: books-db
    volumes:
      - /Users/veykerbarrionuevo/P.Distribuida/db:/var/lib/postgresql/data
    ports:
      - 54321:5432 # Mapeado al 54321 de tu Mac
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres -d books-db" ]
      interval: 5s
      timeout: 5s
      retries: 5

  postgresql-exporter:
    image: prometheuscommunity/postgres-exporter:v0.15.0
    container_name: postgresql-exporter
    environment:
      DATA_SOURCE_NAME: "postgresql://postgres:Badboy22@db-server:5432/books-db?sslmode=disable"
    ports:
      - 9187:9187
    depends_on:
      db-server:
        condition: service_healthy

  consul:
    image: consul:1.15.4
    command: ["agent", "-dev", "-ui", "-bind=0.0.0.0", "-client=0.0.0.0"]
    ports:
      - 8500:8500 # Agregamos la exposición del puerto para que puedas ver el UI de Consul
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8500/v1/status/leader"]
      interval: 5s
      timeout: 5s
      retries: 5

  proxy:
    image: traefik:3.7.5
    container_name: proxy-1
    command:
      - "--api.insecure=true"
      - "--api.dashboard=true"
      - "--entryPoints.http.address=:80"
      - "--entryPoints.traefik.address=:8080"
      - "--entryPoints.metrics.address=:8082"
      - "--providers.consulcatalog=true"
      - "--providers.consulcatalog.endpoint.address=consul:8500"
      - "--providers.consulcatalog.exposedByDefault=false"
      - "--providers.consulcatalog.refreshInterval=5s"
      - "--metrics.prometheus=true"
      - "--metrics.prometheus.entryPoint=metrics"
    ports:
      - "8089:80"       # Mapeado al 8089 de tu Mac para esquivar tu Nginx local
      - "8088:8080"     # Dashboard de Traefik en el puerto 8088
      - "8082:8082"     # Puerto de métricas de Traefik para Prometheus
    depends_on:
      consul:
        condition: service_healthy

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus-1
    volumes:
      - ./configs:/etc/prometheus
    ports:
      - 9090:9090
    expose:
      - 9000
    depends_on:
      consul:
        condition: service_healthy

  graphana:
    image: grafana/grafana-oss:latest
    container_name: graphana-1
    ports:
      - 3000:3000

  authors:
    build:
      context: ../../app-authors
      dockerfile: src/main/docker/Dockerfile.jvm
    container_name: authors-app
    environment:
      QUARKUS_DATASOURCE_JDBC_URL: jdbc:postgresql://db-server:5432/books-db
      QUARKUS_DATASOURCE_USERNAME: postgres
      QUARKUS_DATASOURCE_PASSWORD: Badboy22
      QUARKUS_HTTP_PORT: 8080     # Internamente escucha en el 8080
      CONSUL_HOST: consul         # Corregido
      CONSUL_PORT: 8500
    ports:
      - 8070:8080                 # Mapeado al 8070 de tu Mac
    depends_on:
      db-server:
        condition: service_healthy
      consul:
        condition: service_healthy

  books:
    build:
      context: ../../app-books
      dockerfile: src/main/docker/Dockerfile.jvm
    container_name: books-app
    environment:
      QUARKUS_DATASOURCE_JDBC_URL: jdbc:postgresql://db-server:5432/books-db
      QUARKUS_DATASOURCE_USERNAME: postgres
      QUARKUS_DATASOURCE_PASSWORD: Badboy22
      QUARKUS_HTTP_PORT: 8080     # Internamente escucha en el 8080
      CONSUL_HOST: consul         # Corregido
      CONSUL_PORT: 8500
    ports:
      - 8081:8080                 # Mapeado al 8081 de tu Mac
    depends_on:
      db-server:
        condition: service_healthy
      consul:
        condition: service_healthy

  customers:
    build:
      context: ../../app-customers
      dockerfile: src/main/docker/Dockerfile.jvm
    container_name: customers-app
    environment:
      SERVER_PORT: 8090           # Sincronizado con el 8090 de tu application.yml
      SPRING_CLOUD_CONSUL_HOST: consul
      SPRING_CLOUD_CONSUL_PORT: 8500
    ports:
      - 8083:8090                 # Mapea el 8083 de tu Mac al 8090 interno del contenedor
    depends_on:
      consul:
        condition: service_healthy

volumes:
  postgres-data: