server:
    port: 8080
    host: "0.0.0.0"
db:
    source: jdbc
    connection:
        url: jdbc:postgresql://localhost:5432/books20252026
        username: postgres
        password: ${DB_PASSWORD}
        poolName: pool1